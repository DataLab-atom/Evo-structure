---
name: freeze
description: "Freeze a branch — stop expanding it in future generations"
---

# /freeze — Freeze Branch

Call `mcts_freeze_branch` with the branch name from `$ARGUMENTS`.

Confirm to user: "Frozen {branch}. It will no longer be selected as a frontier node."
