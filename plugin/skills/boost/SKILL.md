---
name: boost
description: "Boost a branch — raise its UCB priority in the next generation"
---

# /boost — Boost Branch

Call `mcts_boost_branch` with the branch name from `$ARGUMENTS`.

Report to user: "Boosted {branch}. Its UCB priority is now raised for the next generation."
